#include "deck.h"

/**
 * sort_deck - sorts a deck of cards in ascending order.
 * @deck: pointer to a pointer to a deck_node_t.
 *
 * Return: Void
 */
void sort_deck(deck_node_t **deck)
{
	if ((*deck) == NULL || (*deck)->next == NULL)
		return;

}
