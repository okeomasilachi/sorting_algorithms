#include "sort.h"

/**
 * quick_sort_hoare - sort an array using the quick sort hoare algorithm.
 * @array: array to be sorted.
 * @size: number of elements in the array
 *
 * Return: void
 */
void quick_sort_hoare(int *array, size_t size)
{
	if (array == NULL || size < 2)
		return;
}
